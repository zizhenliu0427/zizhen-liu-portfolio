# 性能优化集成记录

2026-09-12，用户授权将优化分支合入网页 main、原壁纸分支，并确认同步独立的 RhineLabWallpaper 仓库。

集成内容为 `4d2846c` 的渲染优化与 `2817b35` 的三档模型对照。网页与独立壁纸仓库移入这两个提交，保留各自已有字体部署、壁纸属性和发布设置。旧 `codex/wallpaper-engine` 按优化分支快进更新。

生产画面继续使用原始高精度模型。中／低模型、Blender 源场景、对照页和测试数据随源代码保存，尚未变更为正式画质选项。用户本次授权是合入已有工作，不隐含新增选项或更改默认档位。

网页 main 构建、渲染更新检查、阵列覆盖检查通过。独立壁纸构建、渲染更新检查、登录身份与定时主题逻辑检查通过；Edge 开场 HUD 回归及真实 Wallpaper Engine 的中英混排姓名、主题文字颜色、定时边界切换回归通过。

原始基准报告继续保存原测试环境与结论，不以合入后的版本号改写历史数据。`build-performance.mjs` 支持 `PERF_BASELINE` 指定本仓库基线；原始提交不可用时自动选择本仓库优化提交的父提交，并输出、保存实际基线 ID。
